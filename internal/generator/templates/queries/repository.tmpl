// {{.RepositoryName}} provides database operations for {{.SourceFile}} queries
type {{.RepositoryName}} struct {
	conn *pgxkit.Connection
}

// New{{.RepositoryName}} creates a new {{.RepositoryName}}
func New{{.RepositoryName}}(conn *pgxkit.Connection) *{{.RepositoryName}} {
	return &{{.RepositoryName}}{
		conn: conn,
	}
} 